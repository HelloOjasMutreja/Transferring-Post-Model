<%= form_with(model: positive_post) do |form| %>
  <% if positive_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(positive_post.errors.count, "error") %> prohibited this positive_post from being saved:</h2>

      <ul>
        <% positive_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
